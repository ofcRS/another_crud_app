# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

union EntityData = ImageData | LinkData

type ApiResponse {
    error: String!
    ok: Boolean!
}

type ContentBlock {
    depth: Float!
    entityRanges: [String!]!
    inlineStyleRanges: [String!]!
    key: String!
    text: String!
    type: String!
}

type EntityMap {
    data: EntityData!
    entity: entityType!
    mutability: Mutability!
}

type ImageData {
    type: entityType!
    url: String!
}

type LinkData {
    type: entityType!
    url: String!
}

type LoginResponse {
    accessToken: String!
    user: User!
}

type Mutation {
    addPost(body: String!, title: String!): Post!
    deletePost(id: Float!): ApiResponse!
    login(email: String!, password: String!): LoginResponse!
    register(email: String!, password: String!): Boolean!
    revokeRefreshTokens(id: Float!): Boolean!
}

type Post {
    body: PostBody!
    id: Int!
    title: String!
}

type PostBody {
    blocks: [ContentBlock!]!
    entityMap: [EntityMap!]!
}

type Query {
    hello: String!
    me: User
    posts: [Post!]!
    users: [User!]!
}

type User {
    email: String!
    id: Int!
}

enum Mutability {
    IMMUTABLE
    MUTABLE
    SEGMENTED
}

enum entityType {
    IMAGE
    LINK
}
