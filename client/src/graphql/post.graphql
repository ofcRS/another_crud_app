query Post {
    posts {
        title,
        body,
        id
    }
}

mutation AddPost($title: String!, $body: String!) {
    addPost(title: $title, body: $body) {
        body,
        id,
        title,
    }
}

mutation DeletePost($id: Float!) {
    deletePost(id: $id) {
        ok
    }
}