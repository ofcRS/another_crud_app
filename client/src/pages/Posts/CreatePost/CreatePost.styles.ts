import styled from 'styled-components';

export const Styled = {
    CreatePost: styled.div`
        > div {
            display: flex;
            flex-wrap: wrap;
        }

        textarea {
            resize: none;
        }
    `,
};
