import styled from 'styled-components';

const Button = styled.button`
    cursor: pointer;
`;

const LinkButton = styled(Button)`
    color: #359ff4;
    border: none;
    background: none;
`;

export const Styled = {
    Button,
    LinkButton,
};
