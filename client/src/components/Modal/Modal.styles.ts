import styled from 'styled-components';

export const Styled = {
    Modal: styled.div`
        position: fixed;
        transform: translateX(-50%) translateY(-50%);
        left: 50%;
        top: 50%;

        background: #fff;
        width: max-content;
        height: max-content;
    `,
};
