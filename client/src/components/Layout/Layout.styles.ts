import styled from 'styled-components';

const Layout = styled.div`
    display: flex;
    flex-wrap: wrap;

    background: #fff;
`;

const MainArea = styled.main`
    width: 50vw;
    margin: 0 auto;
`;

export const Styled = {
    Layout,
    MainArea,
};
