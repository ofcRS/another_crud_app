import styled from 'styled-components';

const ImageWrapper = styled.figure`
    max-width: 100%;
    display: flex;
    justify-content: center;

    img {
        max-width: 50%;
    }
`;

export const Styled = {
    ImageWrapper,
};
